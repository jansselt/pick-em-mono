'use client';

import { Plus } from 'lucide-react';
import { Button } from './ui/button';

export function NavMain() {
  return (
    <div>
      <Button>
        <span>New Pick</span>
      </Button>
    </div>
  );
}
